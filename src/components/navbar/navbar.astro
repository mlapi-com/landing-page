---
import Container from "@components/container.astro";
import Link from "@components/ui/link.astro";
import Dropdown from "./dropdown.astro";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";

const menuitems = [
  {
    title: "Why Email Api?",
    path: "/why-email-api"
  },
  {
    title: "Changlog",
    path: "/changlog"
  },
  // {
  //   title: "Use Cases",
  //   path: "#",
  //   children: [
  //     { title: "ChatGPT", path: "#" },
  //     { title: "Dataset Generation", path: "#" },
  //     { title: "E-Learning", path: "#" },
  //   ],
  // },
  // {
  //   title: "Pricing",
  //   path: "/pricing",
  // },
  // {
  //   title: "About",
  //   path: "/about",
  // },
  // {
  //   title: "Docs",
  //   path: "/",
  // },
  // {
  //   title: "Blog",
  //   path: "/blog",
  // },
  // {
  //   title: "Contact",
  //   path: "/contact",
  // },
];
---

<Container>
  <header class="flex flex-col lg:flex-row justify-between items-center prose-xl sticky pt-6 lg:pt-0">
    <Astronav>
      <div class="flex w-full lg:w-auto items-center justify-between">
        <a href="/" class="text-lg"
          ><span class="font-extrabold text-slate-800 text-3xl">Email 📬 Api </span><span
            class="text-slate-500"></span
          >
        </a>
        <div class="block lg:hidden">
          <MenuIcon class="w-4 h-4 text-gray-800" />
        </div>
      </div>
      <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
        <ul class="flex flex-col lg:flex-row lg:gap-1 ml-96">
          {
            menuitems.map((item, index) => (
              <>
                {item.children && (
                  <Dropdown
                    title={item.title}
                    children={item.children}
                    lastItem={index === menuitems.length - 1}
                  />
                )}

                {!item.children && (
                  <li>
                    <a
                      href={item.path}
                      class="flex-1 lg:px-1 underline text-blue-600 hover:text-black">
                      {item.title}
                    </a>
                  </li>
                )}
              </>
            ))
          }
        </ul>
        <div class="lg:hidden flex items-center mt-3 gap-4">
          <!-- <Link href="#" style="muted" block size="md">Schedule a Call </Link> -->
          <Link href="#" size="md" block>Get started for free</Link>
        </div>
      </MenuItems>
    </Astronav>
    <div>
      <div class="hidden lg:flex items-center gap-4">
        <!-- <a href="#">Schedule a Call </a> -->
        <Link 
          href="https://dashboard.visionapi.space/" 
          size="md" 
          class=" justify-center inline-flex items-center gap-2 rounded-full py-3 px-6 text-white font-medium hover:bg-violet-600">
          Cooming Soon
        </Link>
      </div>
    </div>
  </header>
</Container>
